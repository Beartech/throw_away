<%= form_for(@garbage_can) do |f| %>
  <% if @garbage_can.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@garbage_can.errors.count, "error") %> prohibited this garbage_can from being saved:</h2>

      <ul>
      <% @garbage_can.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
